<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application
            android:name="com.tiagohs.script_reader.App"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name="com.tiagohs.script_reader.ui.activities.SplashActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name=".ui.activities.HomeActivity" />
        <activity android:name=".ui.activities.CategoryActivity" />
        <activity android:name=".ui.activities.SearchActivity" />
        <activity android:name=".ui.activities.ReaderActivity" />
        <activity android:name=".ui.activities.ScriptDetailsActivity"
            android:theme="@style/AppTheme.AppThemeWithStatusTransparent" />
        <activity android:name=".ui.activities.AboutActivity"
            android:theme="@style/AboutTheme"/>
    </application>

</manifest>